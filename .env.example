# Local development flags (DEBUG). Copy to `.env` and run:
#   python3 scripts/sync-env.py
#
# Then rebuild. Sim GPS stays hidden until this is true AND enabled in Settings.

ATLASBOUND_ENABLE_SIM_GPS=false
